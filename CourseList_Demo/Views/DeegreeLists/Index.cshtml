@model IEnumerable<CourseList_Demo.Models.DeegreeList>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DeegreeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DeegreeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DeegreeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DeegreeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DeegreeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
